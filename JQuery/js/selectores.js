$(document).ready(function() {
    //Selector de etiqueta
    // $('h1').hide();
    // $('h1, h2').hide();

    //Selector de clases
    // $('.encabezado').hide();

    //Selector de id
    // $('#primero').hide();

    //Seleccionar 
    // Todos los parrafos que tengan el id primero
    // $('p#primero').hide();

    $('.primero a').hide();
});