$(document).ready(function () {
    //.remove() Para eliminar elementos
    // $('#contenedor').remove();
    // $('#contenedor').children().remove();
    // $('#contenedor').children('#segunda').remove();
    $('#contenedor').empty(); //Borra todos los elementos hijo
});